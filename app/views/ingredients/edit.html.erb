<%= fields_for @ingredients do |f| %>
  <label>Ingredient Amount:</label>
  <%= f.text_field :amount %><br>
  <label>Ingredient Name:</label>
  <%= f.text_field :name %><br>
  <%= f.submit 'Edit Ingredients' %>
<% end %>
